# URL of the records API endpoint.
# During development the Vite proxy (vite.config.js) rewrites /api/* → PHP server,
# so you can leave this as the relative path below and never hit CORS.
VITE_RECORDS_API=/api/records.php
